/**
 * Constants for Pomodoro Focus MCP Server
 *
 * Centralizes all configuration values.
 */

/**
 * Server configuration
 *
 * Core MCP server metadata.
 */
export const SERVER_CONFIG = {
  NAME: "Pomodoro Focus",
  VERSION: "1.0.0"
} as const;
